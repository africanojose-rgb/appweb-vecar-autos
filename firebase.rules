rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
<<<<<<< HEAD
    match /vehiculos/{vehicleId} {
      allow read: if request.auth != null || resource.data.visibleWeb == true;
      allow write: if request.auth != null;
=======

    match /vehiculos/{vehicleId} {
      allow read: if request.auth != null || resource.data.visibleWeb == true;

      allow create: if request.auth != null
        && request.resource.data.keys().hasAll(['marca', 'linea', 'placa', 'color', 'anio', 'precioVenta'])
        && request.resource.data.marca is string
        && request.resource.data.linea is string
        && request.resource.data.placa is string
        && request.resource.data.color is string
        && request.resource.data.anio is number
        && request.resource.data.anio >= 1900
        && request.resource.data.anio <= 2100
        && request.resource.data.precioVenta is number
        && request.resource.data.precioVenta > 0
        && (!request.resource.data.keys().hasAny(['createdAt', 'createdBy', 'id']));

      allow update: if request.auth != null
        && request.resource.data.marca is string
        && request.resource.data.linea is string
        && request.resource.data.placa is string
        && request.resource.data.anio is number
        && request.resource.data.anio >= 1900
        && request.resource.data.anio <= 2100
        && request.resource.data.precioVenta is number
        && request.resource.data.precioVenta > 0
        && (!request.resource.data.keys().hasAny(['createdAt', 'createdBy', 'id']));

      allow delete: if request.auth != null;
>>>>>>> f1f0a6b (actualizacion con descarga y backup)
    }
  }
}

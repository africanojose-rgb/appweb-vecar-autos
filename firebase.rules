rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /vehiculos/{vehicleId} {
      allow read: if request.auth != null || resource.data.visibleWeb == true;
      allow write: if request.auth != null;
    }
  }
}
